<div class="card">
  <div class="listview">
    <div class="lv-header">
        Goals Progress
    </div>
    <div class="lv-body scrollable goal-progess">
    <% if @goals %>
      <% @goals.each do |goal| %>
        <div class="lv-item">
            <div class="lv-title m-b-5"><%= goal.name %></div>

            <div class="progress">
                <div class="progress-bar" role="progressbar" aria-valuenow="<%= current_user.overall_percentage_complete %>" aria-valuemin="0" aria-valuemax="100" style="width: <%= current_user.overall_percentage_complete %>%">
                    <span class="sr-only"><%= current_user.overall_percentage_complete %>% Complete (success)</span>
                </div>
            </div>
        </div>
      <%end%>
    <%end%>
    </div>
  </div>
</div>
